using UnityEngine;
using System.Collections;

public class SpeechRecorder : MonoBehaviour {

	/*
	 * if we don't parse properly, could save a clip to the resources that has a better reading
	 * 
	 * */
	public class MicrophoneInput : MonoBehaviour {
		void Start() {
			//AudioClip clip = Microphone.Start(null, true, 10, 44100);
			//
		}
	}
}
